# [3.4.4 - An Overview of DNS](https://www.youtube.com/watch?v=S4s3vgm4h84)

## 3.4.4 - An Overview of DNS

- Day: Day 8
- Duration: 16:54

## Overview

one of the most common uses of DNS or the domain name system is to translate the names that we put into our browsers into IP addresses that can be used to connect with that particular server for example if you put into a browser www.professormesser.com there's no IP address associated with that name there needs to be some process to resolve the IP address based on that name and we rely on DNS to provide us with that name resolution this database of names is a hierarchy and in this video we will look at the structure of a DNS database we often describe this resolution process as one device that is receiving information from one DNS server but in reality there are many DNS

## Core Ideas

- one of the most common uses of DNS or the domain name system is to translate the names that we put into our browsers into IP addresses that can be used to connect with that particular server for example if you put into a browser www.professormesser.com
- there's no IP address associated with that name there needs to be some process to resolve the IP address based on that name and we rely on DNS to provide us with that name resolution this database of names is a hierarchy and in this video we will look at the structure of a
- DNS database we often describe this resolution process as one device that is receiving information from one DNS server but in reality there are many DNS servers in fact the root DNS servers consist of 13 separate server clusters that make up over 1,000 actual DNS
- servers these DNS servers manage the top level domains or tlds of a number of different generic top levels such as.org Donnet and others there are also country level tlds those are country codes such as us for the United States ca for canada.

## What to Remember

- Name resolution, DNS records, and the relationship between names and IP addresses

## Exam Connections

- Be ready for questions about record types, resolution flow, and troubleshooting name lookups.

## Key Takeaways

- one of the most common uses of DNS or
- connect with that particular server for
- the domain name system is to translate
- level domains or tlds of a number of
- example if you put into a browser
